{
  "name": "digital-brain",
  "version": "1.0.0",
  "description": "Bio-inspired digital human brain with SNNs, STDP and functional brain regions",
  "main": "dist/server.js",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc && cp -r src/dashboard dist/",
    "start": "node dist/server.js",
    "dev": "npx tsx src/server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node dist-test/tests/language-loop.test.js && node dist-test/tests/bilingual-lexicon.test.js && node dist-test/tests/vocabulary-learning.test.js && node dist-test/tests/live-thoughts.test.js && node dist-test/tests/hippocampus.test.js && node dist-test/tests/region-activity.test.js && node dist-test/tests/region-drive.test.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
